server:
  port: 3333
spring:
  application:
    name: config
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #nacos作为注册中心地址
      config:
        server-addr: 127.0.0.1:8848 #nacos作为服务中心地址
        file-extension: yml #指定yml格式配置
        group: test_group #指定组为test_group
        namespace: 364dd8eb-f242-4079-baeb-47b60d457526
